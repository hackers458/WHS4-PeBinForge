using System;

internal static class ManagedSample
{
    private static int Main(string[] args)
    {
        Console.WriteLine("PeBinForge managed classification sample: {0}", args.Length);
        return 0;
    }
}
